<h1><%= @post.author %></h1>
 
<p><%= @post.body %></p>
 
<p><%= @post.created_at %></p>

<%= link_to "Edit", edit_post_path(@post) %>

<%= link_to "Delete", post_path(@post), method: :delete, data: {confirm: "Do you want to delete"} %>